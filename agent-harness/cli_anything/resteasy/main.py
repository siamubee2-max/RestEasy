"""RestEasy CLI — entry point."""

from cli_anything.resteasy.resteasy_cli import cli


def main():
    """Main entry point for the RestEasy CLI."""
        cli(prog_name="resteasy")


        if __name__ == "__main__":
            main()
            
